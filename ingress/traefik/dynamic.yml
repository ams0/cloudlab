http:
  middlewares:
    fail2ban:
      plugin:
        fail2ban:
          allowlist:
            ip: ::1,127.0.0.1
          denylist:
            ip: 192.168.0.0/24
          rules:
            bantime: 3h
            enabled: "true"
            findtime: 10m
            maxretry: "4"
            statuscode: 400,401,403-499

    # Guards the Traefik dashboard, which exposes every router, service and
    # middleware on this host. Nothing else here is authenticated, so the
    # dashboard must not be the one open admin surface.
    dashboard-auth:
      basicAuth:
        users:
          - "admin:$2y$05$0xFqkTDzppb9is2jdcbBqu5LxnY6/vjQRBXLnFR1v7bkGCGUQq5aO"

  routers:
    # Each domain gets its own router so Traefik can request
    # individual certs dynamically when new domains are added.
    # To add a new domain: copy any router block, change the name and Host.

    home:
      rule: "Host(`home.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    status:
      rule: "Host(`status.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    # Traefik's own dashboard — served by Traefik itself (api@internal),
    # not proxied into the cluster like every other router here.
    traefik:
      rule: "Host(`traefik.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: api@internal
      middlewares: [dashboard-auth]
      tls:
        certResolver: letsencrypt

    atlantis:
      rule: "Host(`atlantis.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    backstage:
      rule: "Host(`backstage.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    wekan:
      rule: "Host(`wekan.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    rustfs:
      rule: "Host(`rustfs.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    outline:
      rule: "Host(`outline.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    uptime:
      rule: "Host(`uptime.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    draw:
      rule: "Host(`draw.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    alarik-console:
      rule: "Host(`alarik-console.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt 

    alarik:
      rule: "Host(`alarik.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    omni-siderolink:
      rule: "Host(`omni-siderolink.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: omni-h2c-service
      tls:
        certResolver: letsencrypt

    omni-k8s:
      rule: "Host(`omni-k8s.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: omni-h2c-service
      tls:
        certResolver: letsencrypt

    omni:
      rule: "Host(`omni.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: omni-h2c-service
      tls:
        certResolver: letsencrypt

    waha:
      rule: "Host(`waha.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    authentik:
      rule: "Host(`authentik.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    argocd:
      rule: "Host(`argocd.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt
 
    codimd:
      rule: "Host(`codimd.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    todo:
      rule: "Host(`todo.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    coder:
      rule: "Host(`coder.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    chat:
      rule: "Host(`chat.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    talks:
      rule: "Host(`talks.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    next:
      rule: "Host(`next.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    wiki:
      rule: "Host(`wiki.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    claw:
      rule: "Host(`claw.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    supabase:
      rule: "Host(`supabase.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    auth:
      rule: "Host(`auth.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    actual:
      rule: "Host(`actual.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    nginx:
      rule: "Host(`nginx.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    registry:
      rule: "Host(`registry.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    grafana:
      rule: "Host(`grafana.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    loki:
      rule: "Host(`loki.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    dash:
      rule: "Host(`dash.vps.kubespaces.cloud`)"

      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    n8n:
      rule: "Host(`n8n.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    s3:
      rule: "Host(`s3.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    code:
      rule: "Host(`code.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    minecraft:
      rule: "Host(`minecraft.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    echo:
      rule: "Host(`echo.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    webhook:
      rule: "Host(`webhook.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    helix:
      rule: "Host(`helix.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

    rancher:
      rule: "Host(`rancher.vps.kubespaces.cloud`)"
      entryPoints: [websecure]
      service: backend-service
      tls:
        certResolver: letsencrypt

  services:
    # Backend service pointing to localhost:30963 on the host
    backend-service:
      loadBalancer:
        servers:
          - url: "http://host.docker.internal:30963"

    # Omni requires HTTP/2 (h2c) for GRPC communication
    omni-h2c-service:
      loadBalancer:
        servers:
          - url: "h2c://host.docker.internal:30963"